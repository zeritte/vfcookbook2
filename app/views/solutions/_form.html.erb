<%= form_with(model: solution, local: true) do |form| %>
  <% if solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(solution.errors.count, "error") %> prohibited this solution from being saved:</h2>

      <ul>
      <% solution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :before_en %>
    <%= form.text_field :before_en %>
  </div>

  <div class="field">
    <%= form.label :after_en %>
    <%= form.text_field :after_en %>
  </div>

  <div class="field">
    <%= form.label :before_de %>
    <%= form.text_field :before_de %>
  </div>

  <div class="field">
    <%= form.label :after_de %>
    <%= form.text_field :after_de %>
  </div>

  <div class="field">
    <%= form.label :vf_case_id %>
    <%= form.collection_select :vf_case_id, VfCase.order(:name),:id,:name, include_blank: false %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
